<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Define el appender para la consola -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define el appender para el archivo -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/app.log</file> <!-- Ruta y nombre del archivo de log -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Configura el root logger para usar ambos appenders -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT"/> <!-- Asocia el appender de la consola -->
        <appender-ref ref="FILE"/>   <!-- Asocia el appender del archivo -->
    </root>
</configuration>
